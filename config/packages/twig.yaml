twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        gg_recaptcha_site_key: '%env(GOOGLE_RECAPTCHA_SITE_KEY)%'
    form_themes: ['bootstrap_5_layout.html.twig','custom/recaptchaForm.html.twig']

when@test:
    twig:
        strict_variables: true
